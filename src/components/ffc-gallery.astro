---
import Badge from '@/components/ui/badge.astro';
import Button from '@/components/ui/button.astro';
import Icon from '@/components/Icon.astro';

export interface GalleryItem {
  type: 'image' | 'video';
  src: string;
  alt: string;
  title: string;
  subtitle?: string;
  featured?: boolean;
}

// SEO-friendly gallery items with keyword-rich names
export const galleryItems: GalleryItem[] = [
  { type: 'image', src: '/hivy-images/5100/10.webp', alt: 'Swing of LOVE setup photo', title: 'Swing of LOVE', subtitle: '₹5100', featured: true },
  { type: 'image', src: '/hivy-images/5100/11.webp', alt: 'Swing of LOVE setup photo', title: 'Swing of LOVE', featured: false },
  { type: 'image', src: '/hivy-images/5100/12.webp', alt: 'Swing of LOVE setup photo', title: 'Swing of LOVE', featured: false },
  { type: 'image', src: '/hivy-images/5100/13.webp', alt: 'Swing of LOVE setup photo', title: 'Swing of LOVE', featured: false },
  { type: 'image', src: '/hivy-images/5100/14.webp', alt: 'Swing of LOVE setup photo', title: 'Swing of LOVE', featured: false },
  { type: 'image', src: '/hivy-images/5100/15.webp', alt: 'Swing of LOVE setup photo', title: 'Swing of LOVE', featured: false },
  { type: 'image', src: '/hivy-images/5400/10.webp', alt: 'Elite Group Setup setup photo', title: 'Elite Group Setup', subtitle: '₹5400', featured: false },
  { type: 'image', src: '/hivy-images/5400/11.webp', alt: 'Elite Group Setup setup photo', title: 'Elite Group Setup', featured: false },
  { type: 'image', src: '/hivy-images/5400/2.webp', alt: 'Elite Group Setup setup photo', title: 'Elite Group Setup', featured: false },
  { type: 'image', src: '/hivy-images/5400/3.webp', alt: 'Elite Group Setup setup photo', title: 'Elite Group Setup', featured: false },
  { type: 'image', src: '/hivy-images/5400/4.webp', alt: 'Elite Group Setup setup photo', title: 'Elite Group Setup', featured: false },
  { type: 'image', src: '/hivy-images/5400/5.webp', alt: 'Elite Group Setup setup photo', title: 'Elite Group Setup', featured: false },
  { type: 'image', src: '/hivy-images/5700/10.webp', alt: 'BoHo Chic setup photo', title: 'BoHo Chic', subtitle: '₹5700', featured: false },
  { type: 'image', src: '/hivy-images/5700/11.webp', alt: 'BoHo Chic setup photo', title: 'BoHo Chic', featured: false },
  { type: 'image', src: '/hivy-images/5700/12.webp', alt: 'BoHo Chic setup photo', title: 'BoHo Chic', featured: false },
  { type: 'image', src: '/hivy-images/5700/13.webp', alt: 'BoHo Chic setup photo', title: 'BoHo Chic', featured: false },
  { type: 'image', src: '/hivy-images/5700/14.webp', alt: 'BoHo Chic setup photo', title: 'BoHo Chic', featured: false },
  { type: 'image', src: '/hivy-images/5700/15.webp', alt: 'BoHo Chic setup photo', title: 'BoHo Chic', featured: false },
  { type: 'image', src: '/hivy-images/6300/10.webp', alt: 'Fairy Tale Proposals setup photo', title: 'Fairy Tale Proposals', subtitle: '₹6300', featured: false },
  { type: 'image', src: '/hivy-images/6300/11.webp', alt: 'Fairy Tale Proposals setup photo', title: 'Fairy Tale Proposals', featured: false },
  { type: 'image', src: '/hivy-images/6300/2.webp', alt: 'Fairy Tale Proposals setup photo', title: 'Fairy Tale Proposals', featured: false },
  { type: 'image', src: '/hivy-images/6300/3.webp', alt: 'Fairy Tale Proposals setup photo', title: 'Fairy Tale Proposals', featured: false },
  { type: 'image', src: '/hivy-images/6300/4.webp', alt: 'Fairy Tale Proposals setup photo', title: 'Fairy Tale Proposals', featured: false },
  { type: 'image', src: '/hivy-images/6300/5.webp', alt: 'Fairy Tale Proposals setup photo', title: 'Fairy Tale Proposals', featured: false },
  { type: 'image', src: '/hivy-images/6500/10.webp', alt: 'Tent of Romance setup photo', title: 'Tent of Romance', subtitle: '₹6500', featured: false },
  { type: 'image', src: '/hivy-images/6500/11.webp', alt: 'Tent of Romance setup photo', title: 'Tent of Romance', featured: false },
  { type: 'image', src: '/hivy-images/6500/12.webp', alt: 'Tent of Romance setup photo', title: 'Tent of Romance', featured: false },
  { type: 'image', src: '/hivy-images/6500/2.webp', alt: 'Tent of Romance setup photo', title: 'Tent of Romance', featured: false },
  { type: 'image', src: '/hivy-images/6500/3.webp', alt: 'Tent of Romance setup photo', title: 'Tent of Romance', featured: false },
  { type: 'image', src: '/hivy-images/6500/4.webp', alt: 'Tent of Romance setup photo', title: 'Tent of Romance', featured: false },
];

export interface Props {
  title?: string;
  subtitle?: string;
  maxItems?: number;
  showFilters?: boolean;
  class?: string;
}

const {
  title = 'Our Gallery',
  subtitle = 'Real celebrations, real memories',
  maxItems = 12,
  showFilters = true,
  class: className = '',
} = Astro.props;

const filteredItems = galleryItems.slice(0, maxItems);
const photoCount = galleryItems.filter((item) => item.type === 'image').length;
const videoCount = galleryItems.filter((item) => item.type === 'video').length;
const galleryId = `ffc-gallery-${Math.random().toString(36).slice(2, 9)}`;
---
<section class={`py-12 md:py-16 bg-gradient-to-br from-amber-50 via-white to-orange-50 ${className}`} data-ffc-gallery={galleryId}>
  <div class="container mx-auto px-4">
    <div class="text-center mb-8">
      <Badge class="mb-4 bg-amber-100 text-amber-800 border-amber-200">
        <Icon name="image" class="h-4 w-4 mr-2" /> {title}
      </Badge>
      <h2 class="text-2xl md:text-3xl font-bold mb-2 font-serif">
        {subtitle}
      </h2>
      <p class="text-gray-600">
        Browse our collection of romantic celebrations in Surat
      </p>
    </div>

    {showFilters && (
      <div class="flex justify-center gap-3 mb-8" data-gallery-filters>
        <Button data-filter-btn data-filter="all" class="bg-amber-500 hover:bg-amber-700 text-white">
          All ({photoCount + videoCount})
        </Button>
        <Button data-filter-btn data-filter="photos" variant="outline" class="border-amber-300 text-amber-800 hover:bg-amber-50">
          <Icon name="image" class="h-4 w-4 mr-2" />
          Photos ({photoCount})
        </Button>
        <Button data-filter-btn data-filter="videos" variant="outline" class="border-amber-300 text-amber-800 hover:bg-amber-50">
          <Icon name="play" class="h-4 w-4 mr-2" />
          Videos ({videoCount})
        </Button>
      </div>
    )}

    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3 md:gap-4" data-gallery-grid>
      {filteredItems.map((item, index) => (
        <div
          class={`relative group overflow-hidden rounded-xl ${item.featured ? 'col-span-2 row-span-2 gallery-featured-item' : 'aspect-square'}`}
          data-gallery-item
          data-type={item.type === 'image' ? 'photos' : 'videos'}
        >
          {item.type === 'image' ? (
            <>
              <img
                src={item.src}
                alt={item.alt}
                width={item.featured ? 600 : 300}
                height={item.featured ? 600 : 300}
                class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                <div class={`absolute ${item.featured ? 'bottom-4 left-4' : 'bottom-3 left-3'} text-white`}>
                  <p class={item.featured ? 'font-semibold' : 'font-medium text-sm'}>{item.title}</p>
                  {item.subtitle && <p class="text-sm text-white/80">{item.subtitle}</p>}
                </div>
              </div>
            </>
          ) : (
            <>
              <video
                src={item.src}
                muted
                loop
                playsinline
                class="w-full h-full object-cover"
                data-hover-video
              />
              <div class="absolute inset-0 bg-black/30 flex items-center justify-center group-hover:bg-black/10 transition-colors">
                <div class="w-10 h-10 md:w-12 md:h-12 bg-white/90 rounded-full flex items-center justify-center group-hover:scale-110 transition-transform">
                  <Icon name="play" class="h-4 w-4 md:h-5 md:w-5 text-amber-700 ml-1" fill="currentColor" />
                </div>
              </div>
              <div class="absolute bottom-3 left-3 text-white opacity-0 group-hover:opacity-100 transition-opacity">
                <p class="text-sm font-medium">{item.title}</p>
              </div>
            </>
          )}
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  function setupGallery(section: HTMLElement) {
    const filterBtns = section.querySelectorAll<HTMLElement>('[data-filter-btn]');
    const items = section.querySelectorAll<HTMLElement>('[data-gallery-item]');

    function applyFilter(filter: string) {
      items.forEach((item) => {
        const type = item.dataset.type;
        const visible = filter === 'all' || type === filter;
        item.classList.toggle('hidden', !visible);
        // Featured span only makes sense when "all" is active
        item.classList.toggle('gallery-featured-item', item.classList.contains('gallery-featured-item') && filter === 'all');
      });

      filterBtns.forEach((btn) => {
        const isActive = btn.dataset.filter === filter;
        btn.classList.toggle('bg-amber-500', isActive);
        btn.classList.toggle('hover:bg-amber-700', isActive);
        btn.classList.toggle('text-white', isActive);
        btn.classList.toggle('border-amber-300', !isActive);
        btn.classList.toggle('text-amber-800', !isActive);
        btn.classList.toggle('hover:bg-amber-50', !isActive);
      });
    }

    filterBtns.forEach((btn) => {
      btn.addEventListener('click', () => applyFilter(btn.dataset.filter || 'all'));
    });

    section.querySelectorAll<HTMLVideoElement>('[data-hover-video]').forEach((video) => {
      video.addEventListener('mouseenter', () => video.play());
      video.addEventListener('mouseleave', () => {
        video.pause();
        video.currentTime = 0;
      });
    });
  }

  document.querySelectorAll<HTMLElement>('[data-ffc-gallery]').forEach(setupGallery);
</script>
